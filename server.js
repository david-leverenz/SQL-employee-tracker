const db = mysql.createConnection(
    {
        host: "localhost",
        user: "root",
        password: "root",
        database: "employee_tracker"
    }
)

.then
switch(userInput.userInput){
    case 'something':
        queryName;
        break;
    case 'something else':
        queryName;
        break;
}